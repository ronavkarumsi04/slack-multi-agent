global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "slack-agent-app"
    static_configs:
      - targets: ["app:8000"]
    metrics_path: "/dashboard/api/metrics/prometheus"
    scrape_interval: 15s
